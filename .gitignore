# Node
node_modules/
npm-debug.*
yarn-error.*

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Generated playable web build (regenerate: expo export + scripts/build-play.mjs)
docs/play*.html
docs/core-system.html

# Native / 서명 자격증명 (절대 커밋 금지)
*.orig.*
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision
credentials.json

# 로컬 안드로이드 빌드(expo prebuild 산출물) — 이 PC 절대경로(local.properties)·
#   빌드 산출물(apk) 포함, 머신마다 달라 커밋 대상 아님. 필요시 `npx expo prebuild` 재생성.
android/

# Metro
.metro-health-check*

# macOS
.DS_Store

# Env
.env*.local

# Python bytecode cache
__pycache__/
*.pyc

# 임시 웹 빌드(우회 export)
dist2/
